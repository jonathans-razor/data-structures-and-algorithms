// Create a program that reverses a string.
// So, if you pass in "hello world" it should return "dlrow olleh".
// If you pass in "12345" it should return "54321".
// If you pass in "a" it should return "a".
// If you pass in "" it should return "".
// If you pass in null it should return an error.
// If you pass in undefined it should return an error.
// If you pass in 12345 it should return an error.
// If you pass in true it should return an error.   
// If you pass in false it should return an error.
// If you pass in an array it should return an error.   
// If you pass in an object it should return an error.
// If you pass in a function it should return an error.
// If you pass in a number it should return an error.
// If you pass in a float it should return an error.
// If you pass in a string with only spaces it should return an error.
// If you pass in a string with only tabs it should return an error.
// If you pass in a string with only new lines it should return an error.
// If you pass in a string with only carriage returns it should return an error.
// If you pass in a string with only spaces, tabs, new lines, and carriage returns it should return an error.
// If you pass in a string with spaces, tabs, new lines, and carriage returns it should return an error.
// If you pass in a string with spaces, tabs, new lines, and carriage returns it should return an error.
// If you pass in a string with spaces, tabs, new lines, and carriage returns it should return an error.
// If you pass in a string with spaces, tabs, new lines, and carriage returns it should return an error.
// If you pass in a string with spaces, tabs, new lines, and carriage returns it should return an error.
// If you pass in a string with spaces, tabs, new lines, and carriage returns it should return an error.
// If you pass in a string with spaces, tabs, new lines, and carriage returns it should return an error.
// If you pass in a string with spaces, tabs, new lines, and carriage returns it should return an error.









